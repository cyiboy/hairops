<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
android:background="@mipmap/background"
    android:orientation="vertical"
    tools:context="com.example.user.hairr.Login.LoginFragment">

    <EditText
        android:id="@+id/loginEmail"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_above="@id/view"
        android:background="@drawable/edittext_colorless_bg"
        android:fontFamily="@font/roboto"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:textAlignment="center"
        android:textColorHint="@color/darkLight"
        android:textColor="@color/colorWhite"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/loginPassword"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/view"
        android:background="@drawable/edittext_colorless_bg"
        android:fontFamily="@font/roboto"
        android:hint="@string/password"
        android:inputType="textPassword"
        android:textAlignment="center"
        android:textColorHint="@color/darkLight"
        android:textColor="@color/colorWhite"
        android:textSize="20sp" />
<RelativeLayout
    android:id="@+id/view"
    android:layout_margin="20dp"
    android:layout_centerInParent="true"
    android:background="@color/colorWhite"
    android:layout_width="match_parent"
    android:layout_height="1dp"/>

    <Button
        android:id="@+id/loginButton"
        android:layout_centerHorizontal="true"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_below="@id/loginPassword"
        android:layout_marginTop="60dp"
        android:background="@drawable/_dark"
        android:fontFamily="@font/roboto_bold"

        android:foregroundTint="@color/darkop"
        android:text="LOGIN"
        android:textColor="@color/colorWhite"
        android:textSize="20sp" />

</RelativeLayout>
